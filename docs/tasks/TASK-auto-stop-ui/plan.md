# Plan

1. Add `autoStop` defaults to plugin manifests.
2. Add plugin settings properties and localized UI controls.
3. Save `autoStop` object in plugin settings.
4. Extend `sync-noctalia-settings` to copy auto-stop settings into CLI env.
5. Update docs.
6. Verify QML light checks, JSON parse, CLI sync temp-home test.
